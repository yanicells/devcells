@AGENTS.md

## Package manager

Use `pnpm` for all package management and script execution in this project. Do not use `npm` or `yarn`.

## Skills to use

For work in this project, use these skills:

- `caveman` — invoke for relevant tasks it applies to
- `frontend-design` — invoke for frontend/UI design work
- `vercel-react-best-practices` — invoke when writing or modifying React code
